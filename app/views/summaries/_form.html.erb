<%= form_with(model: summary) do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>

  <%= form.label :original_text %>
  <%= form.text_area :original_text %>

  <%= form.submit "Create Summary" %>
<% end %>
